# Montana TUI Common

Common TUI utilities shared between Montana binaries (shadow, montana).

## Overview

This crate provides reusable TUI components:
- `LogEntry` and `LogLevel` for log display
- Helper functions for formatting (bytes, URLs, etc.)
- Common rendering utilities
